fastapi>=0.110
uvicorn[standard]>=0.27
psycopg[binary]>=3.1
psycopg-pool>=3.2
google-genai>=0.8
# Pinned: mcp 2.0 changed the OAuth helper API — create_client_registration_request
# now returns a sync request, so `await client.send(...)` in connections/mcp_oauth.py
# fails with "Attempted to send an sync request with an AsyncClient instance".
# 1.28.1 is the version this code was written and verified against.
mcp==1.28.1
cryptography>=43.0
python-dotenv>=1.0
pydantic[email]>=2.6
python-multipart>=0.0.9
PyJWT>=2.8
bcrypt>=4.1
pypdf>=4.2
python-docx>=1.1
python-pptx>=0.6.23
pytest>=8.0
